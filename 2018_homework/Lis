#include<iostream>
#include<vector>
#include<utility>
using namespace std;

int main()
{
	int n, a,t;
	pair<int, int> p;
	vector<pair<int, int>> gt;
	vector<vector<pair<int,int>>> check;
	cin >> t;
	for (int testcase = 0; testcase < t; testcase++)
	{
		cin >> n;
		for (int i = 0; i < n; i++)
		{
			cin >> a;
			p = make_pair(a, 1);
			gt.push_back(p);
		}
	}
}
